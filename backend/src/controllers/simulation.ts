//import { generateFromDistribution } from "./simulationController";
import { IDistribution } from "../models/Distribution";

const fix = {type: "fixed", value: 42};

//console.log('Fixed', generateFromDistribution(fix));